// **substract 1 to a byte (_-1 mod 256)**

// INPUT: a
// OUTPUT: a-1 (mod 256)
// no overlap allowed

// I have a more efficient code, but this is just a short code, which test a use of ADD

START
  |_ -> ADD(255), END
